ROM_START			equ $0
ROM_SIZE			equ $8000

PALETTE_RAM			equ $e800
PALETTE_RAM_SIZE		equ $800
PALETTE_RAM_ADDRESS_LINES	equ 11
PALETTE_SIZE			equ $20

FIX_TILE_RAM			equ $d000
FIX_TILE_RAM_SIZE		equ $800
FIX_TILE_RAM_ADDRESS_LINES	equ 11

FIX_TILE			equ FIX_TILE_RAM
FIX_TILE_SIZE			equ $400
FIX_TILE_PALETTE		equ PALETTE_RAM + $200
FIX_TILE_ATTR			equ FIX_TILE_RAM + FIX_TILE_SIZE
FIX_TILE_ATTR_SIZE		equ $400

FG_TILE_RAM			equ $d800
FG_TILE_RAM_SIZE		equ $400
FG_TILE_RAM_ADDRESS_LINES	equ 10

FG_TILE				equ FG_TILE_RAM
FG_TILE_SIZE			equ $200
FG_TILE_ATTR			equ FG_TILE_RAM + FG_TILE_SIZE
FG_TILE_ATTR_SIZE		equ $200
FG_TILE_PALETTE			equ PALETTE_RAM + $400

REG_FG_SCROLL_X			equ $f800	; word
REG_FG_SCROLL_Y			equ $f802	; byte

BG_TILE_RAM			equ $dc00
BG_TILE_RAM_SIZE		equ $400
BG_TILE_RAM_ADDRESS_LINES	equ 10

BG_TILE				equ BG_TILE_RAM
BG_TILE_SIZE			equ $200
BG_TILE_ATTR			equ BG_TILE_RAM + BG_TILE_SIZE
BG_TILE_ATTR_SIZE		equ $200
BG_TILE_PALETTE			equ PALETTE_RAM + $600

REG_BG_SCROLL_X			equ $f803	; word
REG_BG_SCROLL_Y			equ $f805	; byte

SPRITE_RAM			equ $e000
SPRITE_RAM_SIZE			equ $800
SPRITE_RAM_ADDRESS_LINES	equ 11
SPRITE_PALETTE			equ PALETTE_RAM + $0

WORK_RAM_CHIP			equ $c000
WORK_RAM_CHIP_SIZE		equ $1000
WORK_RAM_CHIP_ADDRESS_LINES	equ 12

WORK_RAM			equ WORK_RAM_CHIP
WORK_RAM_SIZE			equ WORK_RAM_CHIP_SIZE

REG_SOUND			equ $f806
REG_MAIN_BANK			equ $f808
REG_SCREEN_FLIP			equ $f807
REG_WATCHDOG			equ $f80b

REG_INPUT_P1_JOY		equ $f800
REG_INPUT_P1_BUTTONS		equ $f801
REG_INPUT_P2_JOY		equ $f802
REG_INPUT_P2_BUTTONS		equ $f803
REG_INPUT_SYS1			equ $f804
REG_INPUT_SYS2			equ $f805
REG_INPUT_SYS3			equ $f80f
REG_INPUT_DSW1_L		equ $f806
REG_INPUT_DSW1_H		equ $f807
REG_INPUT_DSW2_L		equ $f808
REG_INPUT_DSW2_H		equ $f809

CHAR_COLON			equ $3a


	macro WATCHDOG
		ld	(REG_WATCHDOG), a
	endm

	macro SOUND_PLAY
		ld	(REG_SOUND), a
	endm

	macro SOUND_STOP
		ld	a, $0
		ld	(REG_SOUND), a
	endm
