ROM_START			equ $000000
	ifd _27C010_
ROM_SIZE			equ $40000
	else
ROM_SIZE			equ $80000
	endif

TILE_RAM			equ $080000
TILE_RAM_SIZE			equ $4000
TILE_RAM_ADDRESS_LINES		equ 14
TILE_RAM_MASK			equ $ffff

FIX_SPRITE_RAM			equ $0c0000
FIX_SPRITE_RAM_SIZE		equ $4000
FIX_SPRITE_RAM_ADDRESS_LINES	equ 14
FIX_SPRITE_RAM_MASK		equ $ff

PALETTE_RAM			equ $180000
PALETTE_RAM_SIZE		equ $10000	; address space, not size (2k)
PALETTE_RAM_ADDRESS_LINES	equ 16
PALETTE_RAM_MASK		equ $ffff
PALETTE_SIZE			equ $80		; size of individual palette

WORK_RAM			equ $1c0000
WORK_RAM_SIZE			equ $4000
WORK_RAM_ADDRESS_LINES		equ 14
WORK_RAM_MASK			equ $ffff

FIX_TILE			equ FIX_SPRITE_RAM
FIX_TILE_SIZE			equ $2000
FIX_TILE_PALETTE		equ PALETTE_RAM

LAYER_A_TILE			equ TILE_RAM
LAYER_A_TILE_SIZE		equ $1000
LAYER_A_TILE_PALETTE		equ PALETTE_RAM + $8000
REG_LAYER_A_SCROLL_X		equ $100000
REG_LAYER_A_SCROLL_Y		equ $100002

LAYER_B_TILE			equ TILE_RAM + $2000
LAYER_B_TILE_SIZE		equ $1000
LAYER_B_TILE_PALETTE		equ PALETTE_RAM + $6000
REG_LAYER_B_SCROLL_X		equ $100004
REG_LAYER_B_SCROLL_Y		equ $100006

SPRITE_RAM			equ FIX_SPRITE_RAM + $2000
SPRITE_RAM_SIZE			equ $2000
SPRITE_PALETTE			equ PALETTE_RAM + $2000

REG_SOUND			equ $14000d
REG_SCREEN_PRIORITY		equ $140011
REG_IRQ2_ACK			equ $140002
REG_IRQ3_ACK			equ $140000
REG_SPRITE_COPY			equ $140008
REG_WATCHDOG			equ $140016

REG_INPUT_P1			equ $140021
REG_INPUT_P2			equ $140023
REG_INPUT_P3			equ $140025
REG_INPUT_P4			equ $140027
REG_INPUT_DSW1			equ $140020
REG_INPUT_DSW2			equ $140022
REG_INPUT_DSW3			equ $140024
REG_INPUT_SYSTEM		equ $140026

SYS_VBLANK_BIT			equ $2
SYS_VBLANK			equ $04

CHAR_COLON			equ '@'

SOUND_NUM_STOP			equ $00

	macro SOUND_PLAY
		move.b	d0, REG_SOUND
	endm

	macro SOUND_STOP
		move.b	#SOUND_NUM_STOP, REG_SOUND
	endm

	macro WATCHDOG
		clr.w	REG_WATCHDOG
	endm
