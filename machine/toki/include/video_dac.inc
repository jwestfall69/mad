; These are tiles which are filled with a single
; color from the palette that we will use for the
; solid color blocks
TILE_B0_NUM		equ $11
TILE_B0_PAL_OFFSET	equ $0
TILE_B1_NUM		equ $1c
TILE_B1_PAL_OFFSET	equ $16
TILE_B2_NUM		equ $1d
TILE_B2_PAL_OFFSET	equ $18
TILE_BA_NUM		equ $1f		; bit all
TILE_BA_PAL_OFFSET	equ $1c
