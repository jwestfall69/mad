	macro ROMSET_PALETTE_SETUP
		move.w	#PALETTE_TEXT_COLOR, PALETTE_RAM_START + $2
		move.w	#PALETTE_TEXT_COLOR, PALETTE_EXT_RAM_START + $2

		move.w	#PALETTE_TEXT_SHADOW_COLOR, PALETTE_RAM_START + $8
		move.w	#PALETTE_TEXT_SHADOW_COLOR, PALETTE_EXT_RAM_START + $8

		move.w	#PALETTE_BACKGROUND_COLOR, PALETTE_RAM_START + $400
		move.w	#PALETTE_BACKGROUND_COLOR, PALETTE_EXT_RAM_START + $400
	endm

SOUND_NUM_BIT_ZERO		equ $31	; oh oh
SOUND_NUM_BIT_ONE		equ $28	; punch?

; video dac
VD_TILE_B036_NUM		equ $8
VD_TILE_B036_PAL_OFFSET		equ $8
VD_TILE_B147_NUM		equ $61f
VD_TILE_B147_PAL_OFFSET		equ $2
VD_TILE_B25A_NUM		equ $561
VD_TILE_B25A_PAL_OFFSET		equ $10
