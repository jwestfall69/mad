	macro ROMSET_PALETTE_SETUP
		move.w	#PALETTE_TEXT_COLOR, PALETTE_RAM_START + $6
		move.w	#PALETTE_TEXT_COLOR, PALETTE_EXT_RAM_START + $6

		move.w	#PALETTE_TEXT_SHADOW_COLOR, PALETTE_RAM_START + $4
		move.w	#PALETTE_TEXT_SHADOW_COLOR, PALETTE_EXT_RAM_START + $4

		move.w	#PALETTE_BACKGROUND_COLOR, PALETTE_RAM_START + $600
		move.w	#PALETTE_BACKGROUND_COLOR, PALETTE_EXT_RAM_START + $600
	endm

SOUND_NUM_BIT_ZERO		equ $25	; fire
SOUND_NUM_BIT_ONE		equ $38	; voice saying one

; video dac test
TILE_B036_NUM			equ $11
TILE_B036_PAL_OFFSET		equ $2
TILE_B147_NUM			equ $404
TILE_B147_PAL_OFFSET		equ $1e
TILE_B25A_NUM			equ $33a
TILE_B25A_PAL_OFFSET		equ $14
TILE_B25A_PAL_OFFSET2		equ $c
